all:
  hosts:
    vm1:
      ansible_host: 172.20.10.5
      ansible_user: sre
      server_description: "Serveur slave (database_slave + backup)"
      ansible_become_timeout: 60  
      ansible_become_flags: "-S"

